# Focused dependencies for the Base44 dev environment.
# The repo's requirements.txt pulls in many unused heavy packages (pandas, numpy,
# litellm from a private URL, AI SDKs, stripe, boto3 ...) that the server never
# imports. This trimmed set is everything server.py / chess_engine.py actually use.
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv==1.2.3
motor==3.3.1
pymongo==4.6.3
PyJWT==2.13.0
httpx==0.28.1
chess==1.11.2
pydantic==2.13.4
python-multipart==0.0.32
