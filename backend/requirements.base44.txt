# Minimal runtime dependencies for the Chess Arena backend (Base44 dev environment).
# The full requirements.txt pulls heavy AI/cloud SDKs (litellm custom wheel, google-genai,
# boto3, pandas, numpy, stripe, ...) that the server never imports. This trimmed set is
# everything server.py / chess_engine.py actually need, pinned to the same versions.
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.13.4
pydantic_core==2.46.4
motor==3.3.1
pymongo==4.6.3
PyJWT==2.13.0
python-dotenv==1.2.3
chess==1.11.2
httpx==0.28.1
python-multipart==0.0.32
