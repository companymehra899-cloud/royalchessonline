# Trimmed runtime deps for the Base44 dev environment.
# Only packages the backend actually imports (server.py + chess_engine.py).
# The full requirements.txt contains many unused heavy packages and a
# custom-URL litellm wheel that is not needed to run the app here.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
python-dotenv==1.2.3
PyJWT==2.13.0
httpx==0.28.1
pydantic==2.13.4
email-validator==2.3.0
python-multipart==0.0.32
chess==1.11.2
watchfiles==1.2.0
