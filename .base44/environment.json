{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GOOGLE_CLIENT_ID",
      "description": "Google OAuth 2.0 Client ID from Google Cloud Console → APIs & Services → Credentials",
      "requiredAtBoot": false
    },
    {
      "name": "GOOGLE_CLIENT_SECRET",
      "description": "Google OAuth 2.0 Client Secret from Google Cloud Console → APIs & Services → Credentials",
      "requiredAtBoot": false
    }
  ]
}
